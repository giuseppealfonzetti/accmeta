% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{init_theta}
\alias{init_theta}
\title{Moment-based starting value}
\usage{
init_theta(DATA, CC = 0.5, MIN_VAR = 1e-04, SHRINK = 0.5)
}
\arguments{
\item{DATA}{An \code{accmeta_data} object, as returned by \code{\link[=set_meta_data]{set_meta_data()}}. Only
its raw counts are read, so an object built without a continuity correction
is fine here.}

\item{CC}{Continuity correction used to build the starting value, applied to
the raw counts of \code{DATA} independently of the correction recorded on it.
It exists to keep the logit transform finite when a study has an empty
cell, and affects the starting value only, never the fit. The default keeps
the starting value a fixed function of the counts, so that fits at
different corrections all set out from the same point.}

\item{MIN_VAR}{Lower bound applied to the eigenvalues of the covariance.}

\item{SHRINK}{Proportion of the average within-study variance subtracted from
the sample covariance. \code{0} leaves the sample covariance untouched.}
}
\value{
A numeric vector of length 9, in the layout documented for
\code{\link[=theta2list]{theta2list()}}.
}
\description{
Builds a starting \code{THETA} from the data. The means are the column means of
the transformed estimates; the covariance is their sample covariance less the
average within-study variance, since the marginal covariance of the estimates
is \eqn{\Sigma_3 + \Gamma_{i,3}}. The result is symmetrised and its
eigenvalues floored to keep it positive-definite.
}
\examples{
th <- c(2.94, -2.2, -0.4, 0.0953, 0.4, -0.5108, 0.3, 0.2, -0.6931)
set.seed(1)
init_theta(set_meta_data(sim_data(15, th, rep(100, 15))))

}
