% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IB.R
\name{fit_ib}
\alias{fit_ib}
\title{Iterative bootstrap bias correction for TLMM}
\usage{
fit_ib(
  DATA,
  H = 100,
  MAX_ITER = 25,
  TOL = 0.2/sqrt(H),
  STEP = 1,
  PRIOR = set_prior(DEGREES = 5),
  SEEDS = NULL
)
}
\arguments{
\item{DATA}{An \code{accmeta_data} object, as returned by \code{\link[=set_meta_data]{set_meta_data()}},
with \code{CC > 0}.}

\item{H}{Number of datasets simulated per iteration.}

\item{MAX_ITER}{Maximum number of iterations.}

\item{TOL}{Convergence tolerance on
\eqn{\max_j |\hat\pi_j - \overline\pi_{H,j}(\theta)|}, the departure from the
matching equation that defines the estimator.}

\item{STEP}{Damping factor \eqn{\gamma \in (0, 1]}.}

\item{PRIOR}{Prior on \eqn{\Sigma_3}, as returned by \code{\link[=set_prior]{set_prior()}}.}

\item{SEEDS}{Integer vector of length \code{H} seeding the simulated datasets. If
\code{NULL}, drawn once and then held fixed.}
}
\value{
A list with components \code{THETA}, the bias-corrected estimate;
\code{PI_HAT}, the uncorrected TLMM estimate; \code{N_ITER}, the number of iterations
used; \code{CONVERGED}, whether the tolerance was met; \code{STOP}, which rule ended
the recursion, one of \code{"tol"}, \code{"plateau"} or
\code{"maxit"}; \code{RESIDUAL}, the largest absolute departure from the matching
equation at the last iteration; \code{PROGRESS}, that departure at every
iteration; \code{PATH}, the iterates, one row per step; \code{FAIL}, the number of
simulated fits that failed every retry, per iteration; \code{DEGEN}, the
proportion of the \code{H} simulated fits whose \eqn{\Sigma_3} was degenerate,
per iteration; and \code{SEEDS}.
}
\description{
Estimates from TLMM estimates might contain bias from up to three distinct sources:
\enumerate{
\item within-study normal approximation; 2) continuity correction; 3) Wishart prior.
Iterative bootstrap removes them all.
}
}
\details{
The \code{H} seeds are drawn once and reused for every \eqn{\theta} and at every
iteration. The state of the random number generator is restored on exit.

The \code{PRIOR} and the continuity correction recorded on \code{DATA} reach \eqn{\hat\pi}
and every simulated fit.

Differently from \code{\link[=fit_tlmm]{fit_tlmm()}} and \code{\link[=fit_tglmm]{fit_tglmm()}}, a penalised \code{PRIOR} is the default here.
}
\examples{
th <- c(2.94, -2.2, -0.4, 0.0953, 0.4, -0.5108, 0.3, 0.2, -0.6931)
set.seed(1)
x <- sim_data(15, th, sample(40:200, 15, TRUE))
fit <- fit_ib(set_meta_data(x, CC = 0.5), H = 20, MAX_ITER = 3)
rbind(TLMM = fit$PI_HAT, IB = fit$THETA)[, 1:3]

}
\seealso{
\code{\link[=fit_tlmm]{fit_tlmm()}} for the auxiliary estimator and \code{\link[=set_prior]{set_prior()}} for the
prior specification.
}
